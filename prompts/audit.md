# Continuity audit

Return JSON only: `{"replacement":"..."}`. Rewrite the supplied previous
response once, fixing only `fix_only`. Preserve tone, events, dialogue, and
character voice wherever they do not cause the listed contradiction. Do not add
or change voluntary user actions, thoughts, dialogue, choices, or decisions.
